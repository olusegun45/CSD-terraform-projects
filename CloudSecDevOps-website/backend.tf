# store the terraform state file in s3
/* terraform {
  backend "s3" {
    bucket    = "bucketname"
    key       = "nme of project eg jupiter-website-ecs.tfstate"
    region    = "us-east-1"
    profile   = "default"
  }
} */